# Acceptance evidence

Cycle `COLLAB-20260921`. Status must be Pass, Fail or Pending with the environment and evidence. Local fixtures, local signed-token tests, deployed endpoints and real human workflows are separate evidence. No real-market profitability claim is made.

| # | Required outcome | Current state | Evidence needed / owner |
| --- | --- | --- | --- |
| 1 | Invite Ali; unverified login rejected; verification single-use and expiring | Pending | Local signed-token/SQLite flow and single-use/expiry tests pass. Real invite delivery and Ali sign-in pending. |
| 2 | Ten wrong passwords lock login and record audit events | Pending | Local actual-hash login lockout plus concurrent DO storage-harness tests pass; deployed integration pending. |
| 3 | Member cannot call owner-only APIs directly | Pending | Local signed Access member requests denied on enabled owner routes; G2 peer re-check passed; deployed checks pending. |
| 4 | Password reset revokes old sessions | Pending | Local reset, replay, stale-proof race and all-session revocation tests pass. Real reset delivery pending. |
| 5 | Avatar rejects non-images/over 2 MB; output 128 px square | Pending | Actual Cloudflare decoder probe: 128 x 128 WebP and invalid/oversize rejection pass; local guarded R2/SQLite tests pass. Real private upload/display pending. |
| 6 | Terms block access and save version/time | Pending | Local handler/SQLite terms gate and immutable version/time receipt pass. Actual browser+D1 flow pending. |
| 7 | Runs UI exactly matches summary/metrics fixtures | Pending | Fable independent JSON/CSV/NDJSON comparison: zero failures;12 API tests pass. Browser visual comparison pending (Chrome blocked local preview). |
| 8 | Stopped heartbeat becomes stale within 45 seconds | Pending | Local atomic replacement, monotonic 45-second stale badge, actual workerd event/revocation probe and expiry regressions pass. Real paper-container stop pending server access. |
| 9 | Board app commit visible in git, agent git update visible in app | Pending | Local adapter/handler/UI conflict, attribution, revocation and idempotency tests pass. Real authorized staging round trip pending; Astra/Fable |
| 10 | Actual installed desktop updates from v0.1.0 to v0.1.1 | Pending | Windows source and unsigned packaging, strict signature/cache/session tests and local workerd/R2 delivery pass. Actual signed installation/update/relaunch remains pending; other platforms remain separate lanes. |
| 11 | Direct outside connection to origin API port fails | Pending | Real server firewall/container/Tunnel evidence and external denial; authorized server operator |

Current evidence: API12 tests and independent G3 sign-off. Account G2 and M3/M4 peer reviews are recorded in communication-log.md. Worker131 tests passed in both Windows and Ubuntu CI at `0e91a9e`; the later13 download regressions also pass. App84 tests,39 syntax checks and all13 imported Git-blob hashes pass; static build and both workflow YAML files validate. Actual local workerd/R2 delivery passed18 checks, including exact installer/manifest bytes, required Content-Length on GET/HEAD and denied requests. The earlier workerd event probe remains applicable. Desktop signature positives use injected attestations; an actual unsigned Windows fixture is rejected. Unsigned package inspection is separate from installed/signing evidence. All platform, compiler and Docker CI passed at `0e91a9e`; later review follow-ups are reported separately. The unsigned NSIS artifact records that reviewed source stage, and is not a claim that later source was packaged or installed. That source-review stage preceded deployment. The production deployment and current limitations are recorded below. Browser visual checks of the signed-in app remain unverified. See DESKTOP-RELEASES.md and each package's scripts for reproducible checks. No milestone is complete until its applicable rows pass or the user explicitly changes scope.


Production checkpoint, 21 September 2026: G4-reviewed `f462cc8` is deployed on the approved single HTTPS origin with D1 migration0001, private R2, managed Turnstile and encrypted secrets. Worker146 tests pass; all14 CI checks at that exact head are green. Seven unauthenticated live paths redirect to Access; ten local workerd requests stop at the host gate. These are separate from the real RS256 verifier tests. The owner explicitly approved a continuing exact-email OTP Access rule; it is saved and the real browser shows its login page. No human account, app-session or email verification is inferred from that page. The configured unsigned Windows development package has been rebuilt and its embedded production origins, hardened fuses and NotSigned status verified. No signed publication, installation or update is claimed. Every real-workflow acceptance row above remains Pending. See `deployment-checkpoint.json` for the current deployment evidence.

Coordination close-out, 21 September 2026: Fable round 14 accepted all seven owner-bootstrap conditions at source `5d3d084`; 17 bootstrap tests passed and a fresh GitHub snapshot confirmed all 14 checks successful at that exact source. Round 15 closed the review at board `ced19e8` with no further reply expected. This does not change the deployed source `f462cc8` or Worker version `e8516426-648e-4437-a0f7-983bb4fd7be8`. The last reported read-only D1 check found zero users and zero invites. The approved owner invitation remains unsent pending private mail delivery recovery; real signup, signed-in browser acceptance and installed desktop acceptance remain unverified. The development desktop package remains unsigned, uninstalled and unpublished. All eleven rows remain Pending.
